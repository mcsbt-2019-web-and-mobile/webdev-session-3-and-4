import React from 'react';
import ReactDOM from 'react-dom';

// Create a component called Hello that accepts a name property and,
// when rendered, returns an <h1> tag with the string hello and the
// given name (for example, "Hello Pepe")

ReactDOM.render(
  //<App />, mount your component here!
  document.getElementById('root')
);
